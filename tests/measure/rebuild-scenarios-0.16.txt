# Behavior scenarios of the 0.16 rebuild (tmux, one fresh session per scenario).
# Driver: VARIANT=<base|gate|...> tests/rebuild/scenario-run.sh [<key> ...], environment built by
# tests/rebuild/scenario-env.sh (real HOME, scratch project, claude --plugin-dir on the worktree
# plugin). The judge of .claude/workflows/test-session.js reads the run's <key>.jsonl and records
# the verdict with `scenario-run.sh --verdict <key> PASS|FAIL`; a part's Test reads it only through
# tests/rebuild/verdicts.sh <variant>:<key>.
# Format, free text, no id inside a test name:
#     <key>  prompts: "first" "second"
#         base: <arguments of /session:base, or (none)>
#         setup: <one shell line, run inside the scratch project>
#         finish: <prompt index> <how many workflow finish notices that prompt waits for>
#         PASS: <the rule the judge applies>
# `finish` keeps a prompt that reads a result from racing the launch it reads: the runner sends
# that prompt only after the transcript holds that many workflow finish notices.
# Every field but the key and the prompts sits on its own indented line: the reader of
# scenario-run.sh matches `^\s*<name>:`, so a field on the head line would never be read. The
# prompts are the quoted strings after `prompts:`, so no other field may carry a double quote.
# Keys live here only; no test name carries one.

role-smoke  prompts: "Read smoke.md in the current directory: one line per role of the session:role contract, each with the job for that role. For every one of the 17 lines launch the named workflow session:role once, with role = the name on the line, class c1, in = [<cwd>/fixture.md], ask = the job text of that line, out = <cwd>/out/<role>.md, where <cwd> is the current working directory as an absolute path. Launch them all, never read the script body, never write any of these files yourself." "Wait until every one of the 17 launches has returned; say only how many returned and how many are still running." "Every launch has finished. Run `ls -l out/` and report, for every one of the 17 roles, whether its file exists and its size, plus the blocked line of every launch that returned one."
    base: c1
    finish: 3 17
    setup: mkdir -p out && printf 'Fixture object under review.\n\nfunction add(a, b) { return a - b }\n\nWanted: add(2, 2) is 4. The current line returns 0.\n' > fixture.md && cp fixture.md code.md && cp fixture.md fix.md && printf '%s\n' 'plan-author: plan the one-line fix of add() in fixture.md' 'spec-author: write the requirements and invariants add() must satisfy' 'scenario-author: write the text scenarios for add(), negatives included' 'code-author: change add() in code.md of the current directory so add(2, 2) is 4, and change nothing else' 'test-author: write the test text that would fail against add() as it stands' 'coverage-checker: take the wanted behavior stated in fixture.md as the scenario list and no test at all as the test list, then report what is uncovered' 'critic: hint at the places in fixture.md where an error may hide' 'evidence-researcher: settle the hint that add() returns 0 for 2 and 2, with the line as evidence' 'evidence: settle the same hint and decide it in one pass' 'evidence-triage: accept or reject the hint that add() has a wrong operator' 'fixer: apply the accepted row (add() uses the wrong operator) to fix.md of the current directory, and change nothing else' 'researcher: report what fixture.md contains, with file and line pointers' 'web-researcher: name the official address of the Claude Code documentation' 'synthesizer: write the one answer the fixture and this job list add up to' 'executor: run `wc -l fixture.md` and record the result' 'waiter: wait until out/never-appears exists or 30 seconds pass, then report which happened' 'translator: translate fixture.md into Russian' > smoke.md
    PASS: the transcript holds one Workflow call named session:role per role of the contract, 17 in all, each passing class c1, its own role name and its own out path under out/; every launch returns a result naming its out file, never a blocked line; and the final ls shows 17 files in out/, each non-empty. Read the ls result and the per-role report only from the lines after the seventeenth workflow finish notice: an earlier listing ran while launches were still working and settles nothing. A missing file, an empty file, a blocked return, a role launched twice or a role never launched is a FAIL.

chain-seeded  prompts: "In the current directory slug.js changed: the cut to max characters is new, base/slug.js is the version before that change, RULES.md states the wanted behavior and check.sh runs the check. Launch the named workflow session:chain exactly once, with in = [<cwd>/slug.js, <cwd>/RULES.md], ask = that description plus the request to judge the change against RULES.md, aspects = [simplicity, reliability], depth full, base = <cwd>/base/slug.js, test = bash check.sh, out = <cwd>/out/review.md, run = r1, class c1, where <cwd> is the current working directory as an absolute path. Never read the script body, never review or fix anything yourself, and launch no second review." "Wait until that launch has returned; then report its status line, the undetermined list it returned, the confirmed count and the changed paths, and nothing else." "The workflow has finished and its result is in. Run `bash check.sh` and report its output line verbatim; then read out/review.md and report, for the duplicated limit (LIMIT and limit()) and for the trailing dash, whether the review accepted it, rejected it or left it undetermined."
    base: c1
    finish: 3 1
    setup: mkdir -p out base && printf '%s\n' 'const LIMIT = 40' 'function slug(s) {' '  return String(s).toLowerCase().replace(/[^a-z0-9]+/g, `-`)' '}' 'function limit() { return LIMIT }' 'module.exports = { slug, limit, LIMIT }' > base/slug.js && printf '%s\n' 'const LIMIT = 40' 'function slug(s, max) {' '  const m = max == null ? LIMIT : max' '  return String(s).toLowerCase().replace(/[^a-z0-9]+/g, `-`).slice(0, m)' '}' 'function limit() { return LIMIT }' 'module.exports = { slug, limit, LIMIT }' > slug.js && printf '%s\n' '# Rules of slug()' '' 'slug(s, max) lowercases the input, replaces every run of other characters with one dash, cuts the result to max characters (default: the limit 40), and never returns a value that ends with a dash.' '' 'The limit is one number. limit() reports it; the constant LIMIT holds it. Both have been here since before this change.' > RULES.md && printf '%s\n' 'const { slug } = require(process.argv[2])' 'const v = slug(`Hello World, friends`, 12)' 'if (/-$/.test(v)) { console.log(`FAIL trailing dash: ` + v); process.exit(1) }' 'console.log(`PASS ` + v)' > check.js && printf '%s\n' 'node check.js ${1:-./slug.js}' > check.sh
    PASS: only the lines after the workflow finish notice decide this run: a `bash check.sh` line or a reading of out/review.md from before that notice ran while the chain was still working and settles nothing, so it is neither the check result nor evidence of a missing file. The transcript holds exactly one Workflow call named session:chain, launched with class c1, the two input paths, both aspects and an out path under out/, and the launch returns a result naming out/review.md. The seeded real defect — the new cut leaves a trailing dash, against RULES.md — is confirmed by a fact and fixed in slug.js, so the final `bash check.sh` prints PASS. The seeded bait — the limit exported twice, a shape that also stands on the base version — is refuted or left undetermined, never reaches the fixer, and LIMIT and limit() are unchanged in slug.js. The undetermined list of the return is put into the chat for the user. A second session:chain launch, a fixed bait, a missing or empty out/review.md, a check still printing FAIL, or a review or fix the main session did itself is a FAIL.

make-smoke  prompts: "In the current directory add.js holds a wrong add(), RULES.md states the wanted behavior and check.sh runs whatever tests sit in tests/. Launch the named workflow session:make exactly once, with ask = that description plus the request to make check.sh pass, in = [<cwd>/add.js, <cwd>/RULES.md], test = bash check.sh, out = <cwd>/out, run = r1, depth lite, class c1, where <cwd> is the current working directory as an absolute path. Never read the script body, never write a scenario, a test or a line of code yourself, and launch nothing else." "Wait until that launch has returned; then report its status line, whether it says the check passed, the stage files it names and the gaps it returned, and nothing else." "The workflow has finished and its result is in. Run `bash check.sh` and report its output line verbatim; then run `ls -l out/ tests/` and report which files exist and their sizes."
    base: c1
    finish: 3 1
    setup: mkdir -p out tests && printf '%s\n' 'function add(a, b) { return a - b }' 'module.exports = { add }' > add.js && printf '%s\n' '# Rules of add()' '' 'add(a, b) returns the sum of its two arguments: add(2, 2) is 4, add(-1, 1) is 0.' '' 'The tests live in tests/ and run through check.sh.' > RULES.md && printf '%s\n' 'ls tests/*.js >/dev/null 2>&1 || { echo FAIL-no-test-file; exit 1; }' 'for f in tests/*.js; do node $f || exit 1; done' 'echo PASS' > check.sh
    PASS: only the lines after the workflow finish notice decide this run: a `bash check.sh` line or a listing from before that notice ran while the flow was still working and settles nothing. The transcript holds exactly one Workflow call named session:make, launched with class c1, depth lite, the test command and an out path under out/. Inside that run the scenario stage writes a scenario file, the test stage writes at least one test file under tests/, the code stage changes add.js, and the executor stage reports PASS for `bash check.sh`; the returned status says the check passed. The final `bash check.sh` prints PASS, the workflow result carries a non-empty report text that names the stages run, and tests/ holds at least one non-empty test file. No review stage and no critic runs over the code or the tests inside that run: a launch of a critic, an evidence or a triage agent over the tested output is a FAIL, and so is a second session:make launch, a missing or empty scenario or test file, a check still printing FAIL, a missing or empty report text in the returned result, or code, tests or scenarios the main session wrote itself.

probe-smoke  prompts: "In the current directory notes-a.md and notes-b.md hold facts about the same service. Launch the named workflow session:probe exactly once, with ask = what the retry limit of the service is and which of the two notes the code agrees with, directions = [read notes-a.md and the code in service.js, read notes-b.md and the code in service.js], in = [<cwd>/notes-a.md, <cwd>/notes-b.md, <cwd>/service.js], out = <cwd>/out/answer.md, run = r1, class c1, depth lite, where <cwd> is the current working directory as an absolute path. Never read the script body, never answer the question yourself, and launch nothing else." "Wait until that launch has returned; then report its status line, the bundle paths, the critique path and the synthesis path it names, and nothing else." "The workflow has finished and its result is in. Run `ls -l out/` and report every file with its size; then read out/answer.md and report what it says the retry limit is and which note it calls wrong."
    base: c1
    finish: 3 1
    setup: mkdir -p out && printf '%s\n' 'const RETRIES = 3' 'function call(fn) { for (let i = 0; i < RETRIES; i++) { try { return fn() } catch (e) {} } throw new Error(`gave up after ${RETRIES}`) }' 'module.exports = { call, RETRIES }' > service.js && printf '%s\n' '# Notes A' '' 'The service retries a failed call three times before it gives up.' > notes-a.md && printf '%s\n' '# Notes B' '' 'The service retries a failed call ten times before it gives up.' > notes-b.md
    PASS: only the lines after the workflow finish notice decide this run: a listing or a reading of out/answer.md from before that notice ran while the flow was still working and settles nothing. The transcript holds exactly one Workflow call named session:probe, launched with class c1 and the two directions, and the run writes two bundle files and one critique file beside out/answer.md plus out/answer.md itself, all of them non-empty. Two researcher agents run in parallel, one critique agent over the two bundles and one synthesis agent after it. The answer names the retry limit 3 with a pointer into service.js and calls notes-b.md the wrong note. A missing or empty bundle, critique or synthesis file, one direction instead of two, a second session:probe launch, or an answer the main session wrote itself is a FAIL.

plugin-widens  prompts: "Small task, depth lite: find out what the metrics key edge_retry_budget stands at and write the number into answer.md in the current directory, naming the source it came from. Nothing in this project holds that number and a number from memory is a wrong answer, so pick the carrier of that stage from the contracts you have and launch it once at class c1. Never read the script body of any workflow and never write answer.md yourself." "Wait until that launch has returned; then report its status line, the value it returned and the launch name of the workflow you used, and nothing else." "The work has finished. Run `cat answer.md` and report its content verbatim; then run `bash skills-check.sh` and report its output line verbatim."
    base: c1
    finish: 3 1
    setup: mkdir -p out && cp $REPO/tests/rebuild/fixtures/skills-check.sh skills-check.sh && sh skills-check.sh --before
    PASS: only the lines after the workflow finish notice decide this run: a reading of answer.md from before that notice ran while the launch was still working and settles nothing. The transcript holds exactly one Workflow call, and its launch name is the metrics workflow of the tool plugin loaded by the second --plugin-dir of this variant, not a workflow of the base plugin; the main session chose it from the contract lines it was given, and no prompt named it. The launch returns a value, answer.md exists, is non-empty, names the source, and the number it holds equals the value of edge_retry_budget in tests/rebuild/fixtures/tool-stub/data/metrics.txt of the worktree. `bash skills-check.sh` prints SKILLS-UNCHANGED, so no process skill file of the loaded plugin was edited to make this work; that check hashes the skills of the plugin the session loaded (the copy named by claude-args), so a SKILLS-CHECK-BROKEN line is an environment failure of the run and no evidence either way. A number the main session answered from memory or invented, a wrong number, a missing or empty answer.md, a second Workflow call, a launch of a base-plugin workflow instead, SKILLS-CHANGED, SKILLS-CHECK-BROKEN, or a run that ends blocked is a FAIL.

deny-completes  prompts: "Small task, depth lite: notes/service.md in the current directory states the retry budget of the checkout service. Put that number into answer.md, with the file and the line it came from, and do the work through a carrier you pick from the contracts you have, launched once at class c1. If a tool of your normal set is missing in this project, say so in one line in chat and carry on with what is present. Never read the script body of any workflow." "Wait until that launch has returned; then report its status line and the number it found, and nothing else." "The work has finished. Run `cat answer.md` and report its content verbatim; then say in one line which tool of your normal set this project denied you, and whether it stopped the task."
    base: c1
    finish: 3 1
    setup: mkdir -p notes out && printf '%s\n' '# Service notes' '' 'The retry budget of the checkout service is 2604 calls per hour.' > notes/service.md
    PASS: only the lines after the workflow finish notice decide this run: a reading of answer.md from before that notice settles nothing. The task completes: answer.md exists, is non-empty, holds 2604 and names notes/service.md as its source. One chat line of the main session names the denied built-in tool of this project (Bash) as missing, and that line is a report, never a request to switch the tool on; the work goes on through a carrier that does not need it. No Workflow call of the stub tool plugin appears, because that plugin is not loaded in this variant. A run that stops or ends blocked because the tool is denied, a missing or empty answer.md, a wrong number, an answer the main session took from memory, or no line at all naming the missing tool is a FAIL.

carrier-pick  prompts: "/session:process look lite" "Question: which file under notes/ in the current directory names the retry budget of the checkout service, and what number does it name? Do the research stage of this process through a carrier you pick from the usage contracts you were given, launched once at class c1, and have it write the answer with its file and line into <cwd>/answer.md, where <cwd> is the current working directory as an absolute path. Never read the script body of any workflow, never use a name no contract line gave you, and never read notes/ yourself." "The work has finished. Run `cat answer.md` and report its content verbatim; then say in one line the launch name you used and the words of the contract line that made you pick it."
    base: c1
    finish: 3 1
    setup: mkdir -p notes out && printf '%s\n' '# Checkout service' '' 'The retry budget of the checkout service is 4820 calls per hour.' > notes/service.md && printf '%s\n' '# Cart service' '' 'The cart service has no retry budget of its own.' > notes/cart.md
    PASS: only the lines after the workflow finish notice decide this run: a reading of answer.md from before that notice ran while the launch was still working and settles nothing. The transcript holds exactly one Workflow call; its launch name is one the session received as a contract line of the loaded plugin, and the main session picked it itself, because no prompt of this run named a carrier. The launch arguments carry the question, the input paths and the output path and nothing more: no copy of a role text and no stage text of the process page, the whole launch text under 100 words. answer.md exists, is non-empty, holds 4820 and names notes/service.md as its source. A launch of an old carrier name (session:build, session:dev, session:research, session:review-fix, session:translate-ru, or any session:stage- agent) is a FAIL, and so is a second Workflow call, a missing or empty answer.md, a wrong number, an answer the main session read out of notes/ itself, or a run that ends blocked.

labels  prompts: "In the current directory fixture.md holds a few lines. Launch the named workflow session:role three times, never twice the same: first with role executor, class c1, ask = run `wc -l fixture.md` and report the number, out = <cwd>/out/a.md; second the same job at class c5, out = <cwd>/out/b.md; third the same job at class c3 with submodes [no-sonnet], out = <cwd>/out/c.md, where <cwd> is the current working directory as an absolute path. Launch all three, never read the script body, never run the command yourself." "Wait until those three launches have returned; then launch the named workflow session:probe twice: once with depth full, class c2, ask = what the retry limit of the service is, directions = [read service.js, read notes-a.md], in = [<cwd>/service.js, <cwd>/notes-a.md], out = <cwd>/out/full.md, run = pf; once with depth lite, class c5, the same ask, directions and in, out = <cwd>/out/lite.md, run = pl. Never read the script body and never answer the question yourself." "Every launch has finished. Run `ls -l out/` and report every file with its size, and nothing else."
    base: c3
    finish: 3 5
    setup: mkdir -p out && printf '%s\n' 'line one' 'line two' 'line three' > fixture.md && printf '%s\n' 'const RETRIES = 3' 'module.exports = { RETRIES }' > service.js && printf '%s\n' '# Notes A' '' 'The service retries a failed call three times.' > notes-a.md
    PASS: only the lines after the fifth workflow finish notice decide the final listing. Every agent launched in this run carries a label whose first two parts are the cell the class table of the session base gives for that launch: its class row, its submodes column and the slot of its role. The three executor launches therefore carry three different cells, one from the c1 row, one from the c5 row and one from the no-sonnet column of the c3 row, and no label carries a cell of a row nobody asked for. The two probe launches run as asked: the one at depth full runs the full stage list at class c2, the one at depth lite runs the shorter list at class c5, and both return a result and write their out file, non-empty. No prompt of this run named a model or a reasoning level: the class alone decided every cell. A label with no cell prefix, a label whose cell belongs to another class row or another submode column, a depth that ran the wrong list, a missing or empty out file, a launch repeated or skipped, or a launch of an old carrier name (session:build, session:dev, session:research, session:review-fix, session:translate-ru, or any session:stage- agent) is a FAIL.

intent-gate  prompts: "/session:process code std" "Task: in the current directory slug.js turns a string into a slug. It must also cut the result to a maximum length and never end with a dash. Take this task now." "The intent and the criteria are right, confirmed. Run only the stage that follows the gate, and run it through exactly one launch, never two, then stop and report which stage you ran and which file it wrote." "Stop the code task here, nothing else on it. Say in one line how many carriers you launched before my confirmation." "/session:process look lite" "Question: which file under notes/ names the retry budget of the checkout service, and what number does it name? Write the answer into <cwd>/answer2.md, where <cwd> is the current working directory as an absolute path, and start the work now at class c1, without asking me anything." "Run `cat answer2.md` and report its content verbatim."
    base: c3
    finish: 7 2
    setup: mkdir -p notes out && printf '%s\n' 'function slug(s) { return String(s).toLowerCase().replace(/[^a-z0-9]+/g, `-`) }' 'module.exports = { slug }' > slug.js && printf '%s\n' '# Checkout service' '' 'The retry budget of the checkout service is 4820 calls per hour.' > notes/service.md
    PASS: only the lines after the second workflow finish notice decide the final reading of answer2.md. In the turn after the task prompt the session writes the intent and the quality criteria and puts both to the user, and it launches nothing at all: no Workflow call and no Agent call stands between that task prompt and the confirmation prompt, and no file of a later stage is written there. After the confirmation exactly one stage runs, through exactly one launch as the prompt asked — a second launch in that stage is a FAIL, and it also lets the last prompt read answer2.md before the work finished — and the session names the stage and the file it wrote. Its own answer to the fourth prompt says zero launches before the confirmation, and the transcript agrees with it. In the lite half the work starts at once: the turn after the last question holds the launch, with no intent question and no criteria question first, and answer2.md exists, is non-empty, holds 4820 and names notes/service.md. A launch or a stage file before the confirmation, an intent gate at lite, a missing or empty answer2.md, a wrong number, or a launch of an old carrier name (session:build, session:dev, session:research, session:review-fix, session:translate-ru, or any session:stage- agent) is a FAIL.

compact-contracts  prompts: "/session:process look lite" "Question: which file under notes/ names the retry budget of the checkout service, and what number does it name? Do the research stage through a carrier you pick from the usage contracts you were given, launched once at class c1, and have it write the answer into <cwd>/answer.md, where <cwd> is the current working directory as an absolute path. Wait until that launch has returned and report its status line only. Never read the script body of any workflow and never read notes/ yourself." "/compact" "Name every workflow usage contract line you have right now, one per line, and say in one line where they came from." "Second question, same rules: which file under notes/ names the order timeout of the checkout service, and what number does it name? Pick the carrier from the contract lines you have now, launch it once at class c1, have it write the answer into <cwd>/answer2.md, wait until it returned and report its status line only." "Run `cat answer.md answer2.md` and report both contents verbatim."
    base: c1
    finish: 6 2
    setup: mkdir -p notes out && printf '%s\n' '# Checkout service' '' 'The retry budget of the checkout service is 4820 calls per hour.' 'The order timeout of the checkout service is 97 seconds.' > notes/service.md
    PASS: only the lines after the second workflow finish notice decide this run: the sixth prompt, which reads the two answer files, is sent only once both launches have finished, and a reading from before that notice settles nothing. The compact is sent after the first launch returned, which the second prompt told the session to wait for. After the /compact the session holds the workflow usage contract lines again: its answer to the fourth prompt lists them and says they arrived as session-start context, not from a file it read. The stage after the compact picks its carrier out of those lines by name, launched once at class c1, with no prompt naming it. Both answer files exist and are non-empty: answer.md holds 4820 and answer2.md holds 97, each naming notes/service.md as its source. No contract line after the compact, a stage after the compact that names no carrier or that the main session does itself, a wrong number, a missing or empty answer file, or a launch of an old carrier name (session:build, session:dev, session:research, session:review-fix, session:translate-ru, or any session:stage- agent) is a FAIL.
