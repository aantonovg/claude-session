# Real return text of the reliability critic, scenario base:chain-seeded, run 20260920-174213,
# stage label ops-lo-critic-reliability, verbatim except for the shortened output path and the
# shortened evidence fields. This critic's return did carry its hint lines.
/tmp/project-chain-seeded/out/chain-hints-reliability.md 1874 bytes
HINT | reliability | slug.js:4 | high | New `.slice(0, m)` runs after dash-replacement, so a cut landing on a dash leaves a trailing dash and the never-ends-with-dash rule breaks while the call looks successful. | Run `node -e "console.log(require('./slug.js').slug('abcd efg', 5))"` and compare to RULES.md line 3.
HINT | reliability | slug.js:3-4 | high | `max == null` treats only null/undefined as absent, so `slug(s,0)`, `slug(s,-1)`, `slug(s,NaN)` reach slice and return '' or the full string with no error. | Run the three calls and print the results.
HINT | reliability | slug.js:2-4 | medium | Non-string input is coerced by `String(s)` and the cut returns a plausible slug like `undefined`, so missing input never reports failure. | Run `slug(undefined)` and check check.sh for an absent-input assertion.
HINT | reliability | slug.js:1,3,6 | medium | Default limit read from module constant at call time can disagree with a `LIMIT` value a caller copied earlier, so cut length and `limit()` report two numbers. | Read check.sh for a test comparing `slug(x).length` against `limit()`.
HINT | reliability | slug.js:4 | low | Cut counts UTF-16 code units, so a surrogate pair can be split mid-character and leave an invalid trailing unit no check rejects. | Run the cut over a string ending in an emoji and inspect the tail.
DONE
