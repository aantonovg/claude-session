{
  "name": "toolstub",
  "version": "0.1.0",
  "description": "Stub tool plugin for the behavior runs: one metrics store, one agent that reads it through a shell, one workflow, one contract line. No MCP server.",
  "keywords": [
    "metrics",
    "stub",
    "fixture"
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh ${CLAUDE_PLUGIN_ROOT}/bin/contract.sh ${CLAUDE_PLUGIN_ROOT}/workflows/metrics.js toolstub",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
