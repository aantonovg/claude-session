# Fork-first rebuild of the session plugin: task list

Source: `reviews/claude_fork_first_architecture_full_dialogue.md` (sections 1, 4-6, 9-15, 17 are
the spec; the transcript is history). Result: session plugin 0.18.0. Kept from 0.17: the ping
monitors and the three ping skills, the class table `c1`-`c5` with its submodes, the Language and
Style sections of the base, the `session:ask` skill, the statusline mode hook, the workflow
contract collector. Rebuilt: the routing (main, fork, helper, workflow), every skill that carried
the old process, every agent, every workflow, the hooks, the tests, the READMEs.

Status marks: `[x]` done in this run, `[ ]` open, `[?]` done but listed in
`2026-09-22-fork-first-decisions.md` as a contested decision.

## A. Design files

- [x] A1 this task list.
- [x] A2 `docs/plans/2026-09-22-fork-first-decisions.md`: every contested decision with the
  options and the default taken; the user reads it after the run.

## B. Library

- [x] B1 `lib/classes.json`: keep the 35-cell table, the submode order, the models and efforts;
  drop `roles`, `ceilings`, `classStep`, `slotDown`, `sizes`; add the `helpers` map (helper name to
  agent type and default slot) and the fixed seats `guide` and `codex`.
- [x] B2 `lib/block.src.js`: rewrite to the pure functions the two workflows need: `submodes`,
  `cellFor`, `helperOpts`, `handback`, `batchRows`, `cellTokens` (kept for the text test).
- [x] B3 `bin/build.sh`: drop the roles, aspects and task-layout stamping; keep block, class table,
  fromBase.
- [x] B4 `lib/build-manifest.json`: targets `workflows/helper.js`, `workflows/batch.js`,
  `base/BASE.md`, `skills/base/SKILL.md`.
- [x] B5 delete `lib/verification.md`, `lib/task-layout.md`, `lib/roles/`, `lib/aspects/`.

## C. Helpers (agents)

One file per helper: contract in the body (takes, returns, never does, result file), tools in the
frontmatter, no model and no effort key.

- [x] C1 `finder` (index of relevant files, symbols, ranges).
- [x] C2 `extractor` (facts and sources from named local material, schema given).
- [x] C3 `web-extractor` (the same over public sources).
- [x] C4 `runner` (a given experiment, test run, build or command set; logs kept).
- [x] C5 `consumer` (follow a document as a new consumer; first blocker).
- [x] C6 `checker` (narrow review against one named property; candidates with evidence).
- [x] C7 `breaker` (counterexample, failing input, minimal reproduction).
- [x] C8 `applier` (replicate an accepted transformation by sample; exceptions list).
- [x] C9 `hand` (a short one-off operation by explicit rules, no method of its own): written, then dropped on the user's annotation of 2026-09-23 (no bare helper).
- [x] C10 `codex` (the codex CLI shim, moved out of `skills/codex/proxy-prompt.md`).
- [x] C11 delete `agents/tools-*.md`.

## D. Workflows

- [x] D1 `workflows/helper.js`: one helper, one contract, one result directory; explicit cell from
  the class row; returns `{status, report, summary}` parsed from the three handback lines.
- [x] D2 `workflows/batch.js`: one helper over many items in parallel, one row per item, a failed
  item stays a row.
- [x] D3 delete `workflows/role.js`, `chain.js`, `make.js`, `probe.js`.

## E. Base and skills

- [x] E1 `base/BASE.md`: keep Start, Language, Style, the class table; rewrite the routing as the
  four blocks MAIN, FORK, HELPER, USING A RESULT (spec 17.2), the helper table, the launch form, the
  result directory and handback, the wait rules, the forbidden list.
- [x] E2 `skills/base/SKILL.md` regenerated by `bin/build.sh`.
- [x] E3 `skills/codex/SKILL.md`: rewrite as one page: which helper kinds go to codex under which
  mode, the prompt file, the launch through `session:helper` with `helper: codex`; delete
  `codex-modes.md` and `proxy-prompt.md`.
- [x] E4 delete `skills/process/` (SKILL.md, core.md, code.md, doc.md, look.md, mr.md, ops.md,
  intent-form.md).
- [x] E5 keep `skills/ask`, `skills/start-ping`, `skills/stop-ping`, `skills/resume-ping`,
  `skills/reset-counter` unchanged.

## F. Hooks and manifest

- [x] F1 `hooks/modes.sh`: modes `base`, `codex`, `reset-counter` only; no `process`; no
  verification-page line and no resume line at SessionStart.
- [x] F2 delete `hooks/ledger-stop.sh`.
- [x] F3 `.claude-plugin/plugin.json`: version 0.18.0, description, hooks: `modes.sh` on the four
  events, one SessionStart contract hook per workflow (`helper.js`, `batch.js`), `@user`,
  `@project`.
- [x] F4 `.claude-plugin/marketplace.json` at the repo root: version and description.

## G. Tests

New suite `tests/plugin/`, entry `tests/plugin/all.sh`; the old `tests/rebuild/` is deleted.

- [x] G1 `build-sync.sh`: `bin/build.sh --check` clean; the shared block byte-identical in both
  workflows; the generated skill equals the base under its frontmatter.
- [x] G2 `block.sh`: node tests of the pure functions (every class row and submode, every helper,
  the fixed seats, the handback parser on good, partial, blocked and malformed text, batch rows).
- [x] G3 `agents.sh`: one agent file per plugin helper of `classes.json`, frontmatter name and
  tools, description under 100 tokens, no model or effort key, the body names `result.md`, the
  three handback lines and `BLOCKED`.
- [x] G4 `contracts.sh`: usage block of each workflow (one block, right after `meta`, 50-150
  tokens, every `A.<arg>` of the script named in it), one SessionStart hook per workflow in
  `plugin.json`, the collector prints one valid JSON line per script.
- [x] G5 `text.sh`: no model name, cell code, or reasoning level outside the generated class table
  and `classes.json` (`cellTokens` of `lib/block.js`, with a mutant run).
- [x] G6 `hooks.sh`: `modes.sh` records `base` and `codex` in both payload shapes, refuses
  `process` and unrelated commands, wipes on PreCompact and reset-counter, prunes; every hook
  command of `plugin.json` exists on disk; no ledger hook.
- [x] G7 `stale.sh`: no live reference to a retired name (old workflows, tool-set agents, process
  skill, verification page, task layout, ledger hook, roles, aspects) under `plugins/`, the
  READMEs and `.claude/workflows/`.
- [x] G8 `release-gate.sh`: 0.18.0 in both version files, README section "The 0.18 set", the
  version log line, the HEAD subject, a clean tree; `--selftest` in `all.sh`.
- [x] G9 `tests/workflows/usage-test.sh`: keep the collector cases (plain, `--hook --file`,
  `--hook --dir`, escaping, override rules, real plugin timing); drop the base-sentence, README
  and branch change-set cases that belonged to the old set.
- [x] G10 keep `tests/monitors/ping-test.sh` and `tests/codex/wrapper-test.sh`; run them.

## H. Documentation and repo

- [x] H1 `plugins/session/README.md`: rewrite (what the plugin is, the helpers, the launch form,
  the result files, classes, the contract hooks, codex, the mode counters, the version log with the
  0.18.0 line).
- [x] H2 root `README.md`: rewrite the summary.
- [x] H3 `docs/tool-plugin/README.md` and template: "carrier" and "stage" wording to "helper".
- [x] H4 `.claude/workflows/memory-gc.js`: `session:tools-edit` to `session:applier`.
- [x] H5 memory: one project memory for the rebuild, index line in `MEMORY.md`.

## I. Verification

- [x] I1 `tests/plugin/all.sh` green; `tests/workflows/usage-test.sh` green;
  `tests/monitors/ping-test.sh` and `tests/codex/wrapper-test.sh` green.
- [x] I2 `claude plugin validate plugins/session` clean.
- [x] I3 smoke tests in real sessions (spec 15.1), 2026-09-23: run 1 (fork launches the helper,
  the path now the exception) and run 2 (main launches `finder` before the fork, the fork launches
  nothing; codex alone on `luna-high`; `checker` paired with `sol-medium`); decisions D12.
- [x] I6 the user's annotations of 2026-09-23 applied: no launch from a fork after its first call,
  no bare helper, two helper groups with slots by trust and difficulty, codex pair modes restored,
  user-level edits (decisions D1, D2, D3, D8, D9, D11).
- [x] I4 release on the user's word of 2026-09-23: commit `session 0.18.0: …`,
  `tests/plugin/release-gate.sh`, push, reinstall on the Mac and the VM.
- [x] I5 `git add reviews/`: approved in the annotations.
