{
  "comment": "Targets of bin/build.sh. block: the file carries the shared-block markers and gets lib/block.js between them. table: the file carries the class-table markers and gets the table of lib/classes.json as markdown between them. fromBase: the target is the named source file, as this run stamped it, under the frontmatter lines of this entry; it carries no markers and is never hand-edited.",
  "targets": [
    { "path": "workflows/helper.js", "block": true },
    { "path": "workflows/batch.js", "block": true },
    { "path": "base/BASE.md", "table": true },
    {
      "path": "skills/base/SKILL.md",
      "fromBase": "base/BASE.md",
      "frontmatter": [
        "name: base",
        "description: \"Session base: main, fork, helpers, classes. Invoke first in every session and again after /compact.\"",
        "disable-model-invocation: true"
      ]
    }
  ]
}
