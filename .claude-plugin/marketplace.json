{
  "name": "claude-session",
  "owner": {
    "name": "Aleksandr Antonov"
  },
  "metadata": {
    "description": "Session modes for the main Claude Code session: forks, pipeline, pipeline-codex, ask; the worker pool as an unstable experiment.",
    "version": "1.0.0",
    "pluginRoot": "./plugins"
  },
  "plugins": [
    {
      "name": "session",
      "source": "./plugins/session",
      "description": "Session modes as user-invocable skills: forks (main + fork subagents), pipeline (forks plus a staged pipeline for one task with a cost ledger), pipeline-codex (codex models on pipeline stages), ask (non-blocking questions), pool-* (unstable). README holds the cache and compact reference.",
      "version": "0.7.0",
      "keywords": [
        "session",
        "modes",
        "cache",
        "forks",
        "pipeline",
        "pipeline-codex"
      ]
    }
  ]
}
