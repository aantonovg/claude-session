{
  "name": "claude-session",
  "owner": {
    "name": "Aleksandr Antonov"
  },
  "metadata": {
    "description": "Session rules for the main Claude Code session: the fork-first base, helpers, codex, ask.",
    "version": "1.0.0",
    "pluginRoot": "./plugins"
  },
  "plugins": [
    {
      "name": "session",
      "source": "./plugins/session",
      "description": "Session rules as user-invocable skills: base (main keeps the intent, a fork does the work, fresh helpers bring one result each, classes and slots), codex (codex models behind a helper), ask (non-blocking questions), the ping skills. README holds the helper catalog and the result-file protocol.",
      "version": "0.18.0",
      "keywords": [
        "session",
        "base",
        "cache",
        "forks",
        "helpers",
        "codex"
      ]
    }
  ]
}
